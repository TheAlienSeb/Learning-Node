
const _ = require('lodash')

const items = [1,[2,[3,[4]]]]
const newItems = _.flattenDeep(items)

console.log(newItems)
console.log('hello people')

//lodash is a module with functions. 
/*

Here I learned about nodemon - a run time that automatically runs after every change
I learned about package-lock json file which locks the versions of the software used so that way the product is able to work as intended

I also learned about installing locally vs installing globally, and how to optimally use gitignore

*/